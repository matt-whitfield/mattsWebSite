---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import StyledLink from '../components/StyledLink.astro';
import ContactCTA from '../components/ContactCTA.astro';

// Get case studies from content collection
const caseStudies = await getCollection('case-studies');

// Sort case studies by publish date (newest first)
caseStudies.sort((a, b) => {
  const dateA = a.data.publishDate ?? new Date(0);
  const dateB = b.data.publishDate ?? new Date(0);
  return dateB.getTime() - dateA.getTime();
});

// Define industry categories based on tags in case studies
const industryCategories = [
  { label: "All", value: "all" },
  { label: "Food & Beverage", value: "food" },
  { label: "Services", value: "services" },
  { label: "E-commerce", value: "ecommerce" },
  { label: "Lead Generation", value: "leads" },
];
---

<Layout 
    title="Work | Matt Whitfield - Web Designer & Developer"
    description="Portfolio of web design and development projects by Matt Whitfield. See examples of custom websites created for small businesses, restaurants, wineries, and creative teams."
    image="/images/david-matos-RYPYbRoZp0o-unsplash.jpg"
>
    <main class="bg-white">
        <!-- Hero Section with Full-Width Background -->
        <section class="w-full relative pt-32 pb-20">
            <!-- Full-width background gradient that spans the entire section width -->
            <div class="absolute inset-0 w-full h-full bg-gradient-to-br from-white via-white to-purple/10"></div>
            
            <div class="page-container relative z-1">
                <h1 class="neo-heading text-5xl md:text-6xl lg:text-7xl mb-8 text-text">My Work</h1>
                
                <div class="neo-card-base py-10 px-8 md:px-12 mt-8 mb-12 relative max-w-prose">
                    <p class="neo-text text-lg mb-6">
                        Selected web design and development projects for small businesses, creative teams, and entrepreneurs.
                    </p>
                    
                    <div class="absolute -top-6 -right-6 p-2 bg-purple border-3 border-black text-white font-serif font-bold text-base uppercase rotate-3">
                        Portfolio
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Projects Grid -->
        <section class="w-full relative py-16">
            <div class="absolute inset-0 w-full h-full bg-gradient-to-br from-white via-white to-coral/10"></div>
            
            <div class="page-container relative z-1">
                <!-- Filter Section -->
                <div class="flex flex-wrap gap-4 justify-center mb-12">
                    {industryCategories.map((category) => (
                        <button 
                            class="neo-link-base neo-link-secondary font-serif text-sm py-2 px-4" 
                            data-category={category.value}
                        >
                            {category.label}
                        </button>
                    ))}
                </div>
                
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {caseStudies.map((study) => (
                        <div 
                            class={`project-card neo-card-base flex flex-col overflow-hidden p-0 neo-service-card-hover-${study.data.color} h-full`}
                            data-tags={study.data.tags.join(' ').toLowerCase()}
                        >
                            <div class="w-full aspect-video overflow-hidden">
                                <img 
                                    src={study.data.image} 
                                    alt={`${study.data.title} website thumbnail`} 
                                    class="w-full h-full object-cover border-b-3 border-black"
                                />
                            </div>
                            <div class="p-6 flex flex-col flex-grow">
                                <div class="mb-3 flex flex-wrap gap-2">
                                    {study.data.tags.slice(0, 1).map((tag) => (
                                        <span class={`inline-block text-xs font-serif font-medium uppercase tracking-wider py-1 px-2 border-2 border-${study.data.color} bg-${study.data.color} text-${study.data.color === 'highlight' || study.data.color === 'mint' ? 'black' : 'white'}`}>
                                            {tag}
                                        </span>
                                    ))}
                                </div>
                                <h3 class="neo-heading text-xl mb-3">{study.data.title}</h3>
                                <p class="neo-text mb-6 flex-grow">{study.data.description}</p>
                                <StyledLink 
                                    href={`/case-studies/${study.slug}`} 
                                    variant={`secondary-${study.data.color}`} 
                                    class="self-start mt-auto"
                                >
                                    View Project
                                </StyledLink>
                            </div>
                        </div>
                    ))}
                </div>
                
                <!-- Load More Button - Commented out for now as we're showing all case studies 
                <div class="flex justify-center mt-16">
                    <button class="neo-link-base neo-link-black">Load More Projects</button>
                </div>
                -->
            </div>
        </section>
        
        <!-- Process Section -->
        <section class="w-full relative py-20">
            <div class="absolute inset-0 w-full h-full bg-gradient-to-br from-white via-white to-blue/10"></div>
            
            <div class="page-container relative z-1">
                <div class="neo-card-base py-12 px-8 md:px-12 relative">
                    <div class="absolute -top-6 -left-6 p-2 bg-blue border-3 border-black text-white font-serif font-bold text-xl uppercase">
                        How I work
                    </div>
                    
                    <h2 class="neo-heading text-3xl md:text-4xl mb-8 text-text">My Design & Development Process</h2>
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                        <div class="relative">
                            <div class="absolute -top-2 -left-2 w-8 h-8 rounded-full bg-white border-3 border-black flex items-center justify-center font-serif font-bold">
                                1
                            </div>
                            <div class="neo-card-base p-6 border-blue">
                                <h3 class="font-serif font-bold text-xl mb-2">Discovery</h3>
                                <p class="neo-text">Understanding your business goals, audience, and technical requirements</p>
                            </div>
                        </div>
                        
                        <div class="relative">
                            <div class="absolute -top-2 -left-2 w-8 h-8 rounded-full bg-white border-3 border-black flex items-center justify-center font-serif font-bold">
                                2
                            </div>
                            <div class="neo-card-base p-6 border-blue">
                                <h3 class="font-serif font-bold text-xl mb-2">UX & Design</h3>
                                <p class="neo-text">Wireframes, user flows, and visual designs that engage your audience</p>
                            </div>
                        </div>
                        
                        <div class="relative">
                            <div class="absolute -top-2 -left-2 w-8 h-8 rounded-full bg-white border-3 border-black flex items-center justify-center font-serif font-bold">
                                3
                            </div>
                            <div class="neo-card-base p-6 border-blue">
                                <h3 class="font-serif font-bold text-xl mb-2">Development</h3>
                                <p class="neo-text">Clean, efficient code with the right tech for your specific needs</p>
                            </div>
                        </div>
                        
                        <div class="relative">
                            <div class="absolute -top-2 -left-2 w-8 h-8 rounded-full bg-white border-3 border-black flex items-center justify-center font-serif font-bold">
                                4
                            </div>
                            <div class="neo-card-base p-6 border-blue">
                                <h3 class="font-serif font-bold text-xl mb-2">Launch & Support</h3>
                                <p class="neo-text">Thorough testing, smooth deployment, and ongoing maintenance</p>
                            </div>
                        </div>
                    </div>
                    
                    <div class="mt-12 flex justify-center">
                        <StyledLink href="/services" variant="secondary-blue">Learn About Services</StyledLink>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Testimonials Section -->
        <section class="w-full relative py-20">
            <div class="absolute inset-0 w-full h-full bg-gradient-to-br from-white via-white to-highlight/10"></div>
            
            <div class="page-container relative z-1">
                <div class="neo-card-base py-12 px-8 md:px-12 relative">
                    <div class="absolute -top-6 -left-6 p-2 bg-highlight border-3 border-black text-black font-serif font-bold text-xl uppercase">
                        Testimonials
                    </div>
                    
                    <h2 class="neo-heading text-3xl md:text-4xl mb-10 text-text">What Clients Are Saying</h2>
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                        <!-- Testimonial 1 -->
                        <div class="neo-card-base p-6 border-highlight">
                            <blockquote class="neo-text text-lg italic mb-6">
                                "Matt transformed our outdated site into a beautiful, functional hub for our restaurant. Online orders increased dramatically, and customers constantly compliment our new website."
                            </blockquote>
                            <div class="flex items-center">
                                <div class="w-12 h-12 rounded-full overflow-hidden mr-4">
                                    <div class="w-full h-full bg-highlight flex items-center justify-center font-serif font-bold text-xl">JD</div>
                                </div>
                                <div>
                                    <p class="font-serif font-bold">Jamie Davis</p>
                                    <p class="text-sm">Fresh Bistro, Owner</p>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Testimonial 2 -->
                        <div class="neo-card-base p-6 border-highlight">
                            <blockquote class="neo-text text-lg italic mb-6">
                                "Working with Matt was a breeze. He understood our winery's needs immediately and created a site that perfectly represents our brand while making it easy for guests to book tastings."
                            </blockquote>
                            <div class="flex items-center">
                                <div class="w-12 h-12 rounded-full overflow-hidden mr-4">
                                    <div class="w-full h-full bg-coral flex items-center justify-center font-serif font-bold text-xl">MR</div>
                                </div>
                                <div>
                                    <p class="font-serif font-bold">Melissa Roberts</p>
                                    <p class="text-sm">Coastal Winery, Marketing Director</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Contact CTA Section -->
        <section class="w-full relative py-28">
            <div class="absolute inset-0 w-full h-full bg-gradient-to-br from-white via-white to-primary/10"></div>
            
            <div class="page-container relative z-1">
                <ContactCTA
                    title="Ready to create something amazing for your business?"
                    description="Let's talk about your project goals and how I can help you build a website that delivers real results."
                    ctaText="start a project"
                    ctaLink="/contact"
                    boxTitle="Let's create"
                    color="primary"
                />
            </div>
        </section>
    </main>
</Layout>

<style>
    .aspect-video {
        aspect-ratio: 16/9;
    }
</style>

<script>
// Category filtering functionality
document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('[data-category]');
    const projectCards = document.querySelectorAll('.project-card');
    
    filterButtons.forEach(button => {
        button.addEventListener('click', () => {
            const category = button.getAttribute('data-category');
            
            // Highlight active button
            filterButtons.forEach(btn => btn.classList.remove('bg-primary', 'text-white'));
            button.classList.add('bg-primary', 'text-white');
            
            // If "all" is selected, show all projects
            if (category === 'all') {
                projectCards.forEach(card => {
                    card.style.display = 'flex';
                });
                return;
            }
            
            // Otherwise, filter projects by category
            projectCards.forEach(card => {
                const tags = card.getAttribute('data-tags')?.toLowerCase() || '';
                if (tags.includes(category)) {
                    card.style.display = 'flex';
                } else {
                    card.style.display = 'none';
                }
            });
        });
    });
    
    // Set "All" as active by default
    const defaultButton = document.querySelector('[data-category="all"]');
    if (defaultButton) {
        defaultButton.classList.add('bg-primary', 'text-white');
    }
});
</script>
